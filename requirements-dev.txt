-r requirements.txt
pytest==8.3.2
ruff==0.16.4
# scikit-learn/joblib are lightweight enough to include here so
# tests/test_train.py can import train.py's pure functions (evaluate()
# uses sklearn.metrics) without needing the full training environment.
# TensorFlow is NOT here -- it's genuinely heavy, and build_lstm_model()
# lazily imports it only when actually called, so it's never needed just
# to run the test suite. See requirements-training.txt (Part 6) for that.
scikit-learn==1.5.1
joblib==1.4.2